# 打包说明（生成 .exe）

## 成品

| 文件 | 用途 |
|---|---|
| `打包程序\图片转MC像素画投影.exe` | **图形界面版**（推荐）：双击打开，选图片→生成像素画→生成MC投影 |
| `打包程序\图片转MC像素画投影_命令行.exe` | 命令行版：适合批处理/脚本/拖放图片到 exe 上 |

两个都是 PyInstaller 单文件版，无需安装 Python 即可运行。

## 环境

- Windows 10/11
- Python 3.9+（本次在 Python 3.14 + PyInstaller 6.x 下验证通过）
- 依赖：`pip install pyinstaller pillow numpy openpyxl`（openpyxl 用于生成 Excel
  材料清单；numpy 可选，有则加速颜色匹配）

## 重新打包（在项目目录下执行）

```bat
REM 图形界面版（onefile，单文件）
pyinstaller --noconfirm --onefile --windowed ^
  --name "图片转MC像素画投影" --icon "app_icon.ico" main.py

REM 命令行版
pyinstaller --noconfirm --onefile --console ^
  --name "图片转MC像素画投影_命令行" --icon "app_icon.ico" cli.py
```

产物输出到 `dist/`，改名/移动到 `打包程序/` 即可。

> 说明：numpy 为可选依赖（有则加速颜色匹配，无则回退纯 Python），若机器未装 numpy
> 打包体积会更小。

## 验证

打包后可运行无界面自检（自动生成测试图并完整走一遍两阶段转换）：

```bat
"图片转MC像素画投影.exe" --self-test C:\some\output\dir
```

成功后输出目录会出现 `self_test_result.txt`，内容为 `SELF TEST OK ...`。

源码验证：

```bat
python 测试\test_pipeline.py    # 管道测试（litemapy 逐方块比对 + NBT 回环）
python 测试\smoke_test_gui.py   # GUI 冒烟测试
```

## 注意事项

- **首次运行慢**：单文件 exe 启动要解压到临时目录，约几秒；介意可用
  `--onedir`（文件夹版）打包。
- **杀毒软件误报**：PyInstaller 单文件 exe 偶被 Defender / 360 误报，添加信任即可。
- **命令行版双击停留**：无终端（双击/拖放）启动时自动停留“按回车键退出”，
  脚本调用加 `--no-pause`。
- 生成 `.litematic` 后：放入 `.minecraft/schematics/`，游戏内 Litematica
  “投影→加载原理图”，配合 litematica-printer 自动建造。
